extends ../layout

block main
    h1= title
    .grid
        each product in products
            .card
                img(src=`/images/${product.image}`)
                h2= product.name
                p Prix: #{product.price}
                p= product.available ? "Disponible" : "Non disponible"
                a.button(href=`products/${product.id}`) Product details
